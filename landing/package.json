{
  "name": "openclaw-triage-landing",
  "version": "0.1.0",
  "description": "Landing page for OpenClaw Triage",
  "scripts": {
    "dev": "npx serve .",
    "build": "echo 'Static site - no build needed'",
    "deploy": "npx vercel --prod"
  },
  "devDependencies": {
    "serve": "^14.2.0",
    "vercel": "^33.0.0"
  }
}
